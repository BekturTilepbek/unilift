# Продакшн-зависимости. Версии зафиксированы — обновлять осознанно.
# Установка:  pip install -r requirements.txt

# --- Ядро ---
Django==5.1.15

# --- Админка для клиента ---
django-unfold==0.96.0

# --- Локализация ru/ky ---
django-modeltranslation==0.20.3

# --- Обработка фото проектов (авто-WebP в нескольких размерах) ---
django-imagekit==6.1.0
Pillow==12.3.0

# --- Формы / защита ---
django-ratelimit==4.1.0

# --- Статика и сервер ---
whitenoise==6.12.0
gunicorn==26.0.0

# --- Конфигурация ---
python-dotenv==1.2.2
dj-database-url==3.1.2

# --- Уведомления о заявках в Telegram (Фаза 4) ---
requests==2.34.2

# --- Отслеживание ошибок (опционально, работает только если задан SENTRY_DSN) ---
sentry-sdk==2.66.1